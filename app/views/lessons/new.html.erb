<% provide(:title, 'New Lesson') %>
<h1>Create New Lesson</h1>

<div class="row">
  <div class="span6 offset3">
    <%= form_for(@lesson) do |f| %>
      <%= render 'fields', f: f %>
      <%= f.submit "Add Lesson", class: "btn btn-large btn-primary" %>
    <% end %>
  </div>
</div>
