<% provide(:title, 'Admin::' + @category.name) %>

<h1><%= @category.name %> </h1>
<br />
<div class="row text-center">
  <%= link_to 'Delete Tag', admin_destroy_path('tag', @category.id), :confirm => 'Are you sure you want to delete this tag?', :method => :delete, class: 'btn btn-mini btn-danger' %>
</div>